module examples.conditionals
import std.console

fn classify(n: i64) -> i64
    if n > 0
        1
    else if n < 0
        -1
    else
        0

fn main() -> i32
    std.console.println(classify(-5))
    0
