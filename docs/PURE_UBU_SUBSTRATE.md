# Pure Ubu substrate — recovery status

The repository has no active `src/runtime/*.c` or `src/runtime/*.h` and the generated C is standalone.
The recovery gate checks this structural property. It does not claim feature parity with the lost historical `ucc-pre1`.
