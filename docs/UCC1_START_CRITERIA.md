# ucc1 start criteria

Do not call a compiler `ucc1` until it is written in `.ubu`, compiled by `ucc0`, depends on real `.ubu` core/std modules, generates standalone C and passes the purity gate.
The recovery-r1 compiler remains `ucc0`.
